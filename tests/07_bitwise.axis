// AXIS Example 07: Bitwise Operations
// AND, OR, XOR und shifts
// expected: 42

func main() -> i32:
    a: i32 = 0b00101010  # 42 in binär
    b: i32 = 0xFF        # 255
    
    # bitwise AND für lower bits
    masked: i32 = a & 0x3F  # untere 6 bits: 42
    
    # mit shifts checken
    shifted: i32 = 21 << 1   # 21 * 2 = 42
    
    when masked == shifted:
        give 42
    
    give 0
