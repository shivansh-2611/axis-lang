mode script

writeln("Hello from AXIS Script!")
x: i32 = 42
writeln(x)

when x > 10:
    writeln("x is big!")
else:
    writeln("x is small")

# Test string concatenation
name: str = "World"
writeln("Hello, " + name + "!")

# Test arithmetic
y: i32 = x * 2
writeln(y)
